type Query {
  hello: String
}

type Mutation {
  signUp(input: UserInput!): User!
  signIn(input: UserInput!): String!
}

input UserInput {
  username: String
  password: String
}

type User {
  id: ID
  username: String
  password: String
}
