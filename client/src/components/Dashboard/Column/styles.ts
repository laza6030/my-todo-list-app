import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  root: {
    backgroundColor: "#de9595",
    width: 300,
    margin: 10,
    padding: 10,
    borderRadius: 10,
  },

  divider: {
    width: "100%",
  },

  addButton: {
    width: "max-content",
    marginRight: "auto",
  },
});
