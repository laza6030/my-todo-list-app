import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  root: {
    backgroundColor: "#de9595",
    width: 200,
    margin: 10,
    padding: 10,
    borderRadius: 10,
  },
});
