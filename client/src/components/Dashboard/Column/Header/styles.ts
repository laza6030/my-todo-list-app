import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  name: {
    fontFamily: "Barlow",
    marginRight: "auto",
    maxWidth: 100,
    whiteSpace: "nowrap",
    overflow: "hidden",
    textOverflow: "ellipsis",
  },
});
