import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  root: {
    marginTop: 20,
    marginLeft: 20,
  },

  button: {
    height: "max-content",
    width: "max-content",
    marginTop: 10,
    backgroundColor: "#deb795",
  },
});
