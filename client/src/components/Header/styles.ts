import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  appBar: {
    backgroundColor: "#bb52c9",
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
  },

  userName: {
    marginLeft: "auto",
    display: "flex",
  },

  userIcon: {
    marginRight: 5,
  },
});
