import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  root: {
    paddingLeft: 5,
    minHeight: 40,
    width: "100%",
    background: "#dbcaca",
    marginBottom: 5,
  },

  icon: {
    marginLeft: "auto",
  },
});
