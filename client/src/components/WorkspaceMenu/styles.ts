import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  root: {
    background: "#f6e0b5",
    width: 150,
    marginTop: 10,
  },

  title: {
    height: 40,
  },

  divider: {
    width: "100%",
  },
});
