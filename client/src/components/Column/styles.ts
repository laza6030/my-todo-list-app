import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  root: {
    backgroundColor: "#de9595",
    width: 300,
    height: "max-content",
    margin: 10,
    marginLeft: 0,
    padding: 10,
    borderRadius: 10,
  },

  divider: {
    width: "100%",
    marginBottom: 10,
  },

  addButton: {
    width: "max-content",
    marginRight: "auto",
  },
});
