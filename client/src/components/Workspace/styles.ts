import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  button: {
    height: "max-content",
    width: "max-content",
    marginTop: 10,
    backgroundColor: "#deb795",
  },
});
