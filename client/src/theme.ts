import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    background: {
      default: "#1874a5",
      paper: "#69bdd2",
    },

    text: {
      primary: "#eeeee4",
      secondary: "#1e81b0",
    },
  },
});
