import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    background: {
      default: "#69bdd2",
      paper: "#eeeee4",
    },

    text: {
      primary: "#000",
      secondary: "#1874a5",
    },
  },
});
